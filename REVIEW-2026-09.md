# รายงานตรวจโค้ด — Global Intelligence (รอบ ก.ย. 2026)

**คำถามตั้งต้น:** "ควรแก้ตรงไหนอีกไหม"
**คำตอบสั้น:** เจอของที่ต้องแก้จริง 12 จุด — **แก้ให้แล้ว 10 จุด** (พร้อมเทสต์ 32 เคส) และเหลือ
**2 เรื่องที่ต้องให้คุณตัดสินใจ** (ไม่ใช่บั๊ก แต่เป็นนโยบาย) กับอีก 5 เรื่องที่เป็น "ปรับได้ก็ดี"

สถานะ: แก้ในไฟล์จริงแล้วในเครื่องนี้ · `npm test` ผ่าน 32/32 · ยังไม่ commit/push

---

## 1) 🔴 บั๊กจริง — แก้แล้ว

| # | อาการที่ผู้ใช้จะเห็น | สาเหตุที่แท้จริง | หลักฐาน | แก้เป็น |
|---|---|---|---|---|
| 1 | ตัวเลข **Nonfarm Payrolls** ขึ้น "ดึงไม่ได้บางช่อง" ทุกครั้งที่ FRED CSV ล่ม | BLS series ที่ตั้งไว้คือ `CES0500000003` = *Average **Hourly Earnings** of All Employees* (รายได้เฉลี่ย/ชม. ≈ 37) **ไม่ใช่จำนวนตำแหน่งงาน** → ตัวกรอง 50,000–500,000 (พันตำแหน่ง) ตัดทิ้งหมด → throw `no usable PAYEMS level` | เอกสาร FRED: `CES0500000003` = "Average Hourly Earnings of All Employees, Total Private"; `PAYEMS` = `CES0000000001` = "All Employees, Total Nonfarm" | เปลี่ยนเป็น `CES0000000001` + เทสต์กันถดถอย (ส่ง series แบบรายได้เฉลี่ยเข้าไปต้อง throw) |
| 2 | ฟอนต์ **Space Grotesk / JetBrains Mono ไม่เคยถูกโหลด** ตัวเลขทุกตัวใช้ฟอนต์ระบบ | `@import url(fonts.googleapis.com…)` ถูกวางไว้ **กลาง** `<style>` — สเปก CSS บังคับว่า `@import` ต้องมาก่อนกฎอื่นทั้งหมด ถ้าไม่ใช่เบราว์เซอร์ **เมินทิ้งทั้งบรรทัด** (ตรวจแล้ว: กฎแรกอยู่ที่ offset 165, `@import` อยู่ที่ 3505) | CSS Cascade & Inheritance spec §3 (`@import` ต้องนำหน้ากฎอื่น), ตรวจ offset จริงในไฟล์ | ย้ายไป `<link rel="stylesheet">` + `preconnect` ใน `<head>` |
| 3 | การ์ด **FOMC countdown ว่างเปล่า** ตั้งแต่ 1 ม.ค. 2027 เป็นต้นไป และวันแถลง FOMC ของปี 2027 จะไม่ถูกใช้ลด TTL แคช (ข้อมูลจะช้าลง 15 นาที → 3 ชม. ในวันแถลง) | รายการวันที่ถูก hardcode ไว้แค่ปี 2026 ใน **สองที่** (`index.html`, `fred-data.js`) ทั้งที่ Fed ประกาศปฏิทินล่วงหน้าถึงปี 2027 แล้ว | ปฏิทิน FOMC 2027: 27 ม.ค. · 17 มี.ค. · 28 เม.ย. · 9 มิ.ย. · 28 ก.ค. · 15 ก.ย. · 27 ต.ค. · 8 ธ.ค. | เพิ่มวันที่ 2027 ทั้ง 8 วันในทั้งสองไฟล์ + เทสต์ตรวจว่าปฏิทินเรียงลำดับและไม่ซ้ำ |
| 4 | ค่าในช่อง **ราคา/ATH ที่พิมพ์เองถูกทับทุกครั้งที่รีเฟรช** ทั้งที่หน้าเว็บเขียนว่า "แก้เองได้เสมอ" | `fetchPrice()` เขียนทับ `priceInput.value` / `athInput.value` ตรงๆ ไม่รู้ว่าผู้ใช้แก้ไว้ | อ่านโค้ด `fetchPrice()` | จำ flag `data-manual` ต่อช่อง (เก็บลง localStorage ด้วย) + ปุ่มใหม่ **"↻ ใช้ราคาสด"** ในกา​ร์ด ATH + แสดงข้อความบอกเมื่อใช้ค่าที่กรอกเอง |
| 5 | ช่องกรอก **BGeometrics token** ในเว็บดูเหมือน "ต้องกรอก" ทั้งที่ดึงผ่าน proxy อัตโนมัติอยู่แล้ว (และข้อความเดิมบอกโควต้า 8 ครั้ง/ชม. แบบกำกวม) | โควต้า BGeometrics เป็นแบบ **ต่อ IP** (8 ครั้ง/ชม. · 15 ครั้ง/วัน) ทุกคนที่เปิดเว็บจึงกินโควต้าตัวเองคนละชุด และแต่ละครั้งยิง 2 รอบ (ยิงตรงที่ต้องโดน CORS บล็อก + ยิงผ่าน allorigins) | หน้า FAQ ของ bgeometrics.com ("Without a token up to 8 requests/hour and 15/day") | เพิ่มฟังก์ชันใหม่ `onchain.js` ทำงานฝั่งเซิร์ฟเวอร์ (ไม่มี CORS, แคช 8 ชม. ใช้ร่วมกันทุกคน) รองรับ env `BGEOMETRICS_TOKEN` และปรับข้อความในหน้าเว็บให้ตรงความจริง |
| 6 | นาฬิกา "อัปเดตล่าสุด" ด้านบน **ไม่แสดงเวลา** ตอนเปิดหน้าแรก | มีสคริปต์ 3 ที่เขียนทับ `#today` และตัวสุดท้ายเขียนแบบไม่ใส่วันที่/เวลา → ค่าที่เห็นคือของตัวสุดท้าย | `grep getElementById('today')` เจอ 3 จุด | เหลือแหล่งเดียว `window.setHeaderStamp()` ใช้ทั้งตอนโหลดและตอนรีเฟรชข่าว |
| 7 | **ข่าวไม่โหลด** เมื่อโควต้า rss2json หมด และข้อความบอกให้ "ไปใส่ RSS2JSON key ใน ตั้งค่าเพิ่มเติม" ซึ่ง **ไม่มีอยู่จริงแล้ว** | ข่าวทุกพาดหัวต้องผ่าน rss2json (โควต้าฟรีต่อ IP) · เมนู "ตั้งค่าเพิ่มเติม" ที่อ้างถึงถูกลบไปแล้วในรุ่นก่อน | อ่านโค้ด + grep ในไฟล์ | `news-feed.js` **อ่าน Google News RSS เองฝั่งเซิร์ฟเวอร์** (ไม่ต้องมี CORS/ไม่ต้องมี key) ใช้ rss2json เป็น fallback เท่านั้น + timeout 8 วิ + จำกัด `q` ≤ 200 ตัวอักษร + แคช CDN 10 นาที · แก้ข้อความในหน้าเว็บให้ตรง |
| 8 | เปิด/รีเฟรชหน้าเว็บ 1 ครั้ง = ยิงข่าว 5 feed + แปล MyMemory ทุกครั้ง (เปลืองโควต้าฟรีทิ้งเปล่า) | `loadCache().then(refreshNews)` เรียกสดทุกครั้งไม่สนใจเวลาแคช และแคชคำแปลอยู่ในหน่วยความจำอย่างเดียว | อ่านโค้ดท้ายสคริปต์ | แคชข่าว 30 นาที (`NEWS_CACHE_TTL_MS`) + ปุ่ม 🔄 ยังบังคับดึงสดได้เสมอ · แคชคำแปลลง `localStorage` (สูงสุด 400 รายการ อายุ 7 วัน) |
| 9 | **Open Interest แปลงเป็น BTC ผิด** ถ้าผู้ใช้แก้ช่องราคาเองไว้ (ใช้ราคาที่กรอกเองหาร OI ของตลาดจริง) | ใช้ `priceInput.value` เป็นตัวหาร | อ่านโค้ด `fetchSentimentLeverage()` | ใช้ `lastLivePrice` (ราคาตลาดล่าสุดที่ดึงได้) เป็นหลัก แล้วค่อย fallback ไปค่าที่ผู้ใช้กรอก |

## 2) 🟡 ความปลอดภัย / ประสิทธิภาพ — แก้แล้วเช่นกัน

- **XSS ผ่าน RSS** — พาดหัวและคำอธิบายข่าวถูกต่อเข้า `innerHTML` แบบดิบ (`<h3>${ev.title}</h3>`) ถ้าพาดหัวมี
  `<img onerror=…>` ก็รันในเบราว์เซอร์ผู้ใช้ได้ → ใส่ `esc()` ครบทุกฟิลด์ที่มาจากภายนอก
  (รวมผลแปลภาษาไทย, Market impact, และ debug log ของ BGeometrics ที่มีข้อความจาก API ภายนอก)
- **สคริปต์ TradingView ~1 MB โหลดทุกครั้งที่เปิดหน้า** แม้ผู้ใช้จะไม่เคยเปิดแท็บ BTC → โหลดเฉพาะตอนกดแท็บ
  BTC ครั้งแรก (หรือเปิดมาที่ `#btc`)
- **ไม่มี deep link**: กด "แชร์ลิงก์" จากแท็บ BTC แล้วคนรับได้แท็บข่าว → เพิ่ม `#btc` / `#news`
  (hash routing + `history.replaceState`)
- **ไม่มี OG/Twitter tags เลย** → แชร์ลง X แล้วขึ้นเป็นลิงก์เปล่า → เพิ่ม meta description, og:*, twitter:*,
  theme-color
- **เหตุผลที่ตัวเลขบนการ์ดไม่เท่ากับพาดหัวข่าว**: ค่า NFP ในหน้านี้คือส่วนต่างเดือนต่อเดือนของระดับ `PAYEMS`
  ที่ปรับฤดูกาลแล้ว (รวมการ revise ย้อนหลัง) → เพิ่มหมายเหตุอธิบายใต้บรรทัด "อัปเดตล่าสุด" เพื่อไม่ให้เข้าใจผิด
- **`oil-price.js`**: เดิมไม่มี timeout เลย (ค้างจนครบ 10 วิ ของ Netlify) และทิ้งข้อความอธิบายของ
  Alpha Vantage (`Note` / `Information` / `Error Message` ที่ส่งมากับ HTTP 200) ทำให้ error ที่ขึ้นคือ
  "No price in Alpha Vantage response" ซึ่งไม่บอกอะไร → เพิ่ม timeout 8 วิ, echo ข้อความต้นทาง,
  ตอบ `503` + hint ชัดเจนเมื่อไม่ได้ตั้ง key, แคชฝั่ง CDN 1 ชม.
- **`fred-data.js`**: แยกตรรกะคิดเลขออกเป็นฟังก์ชันบริสุทธิ์ (`yoyFromLevels`, `payrollsFromLevels`)
  เพื่อเทสต์ได้โดยไม่ต้องออกเน็ต (บั๊ก CPI ต.ค. 2025 เดิมหลุดเพราะเทสต์ไม่ได้)
- **ไฟล์ซอร์สถูกเสิร์ฟเป็นไฟล์สาธารณะ**: เพราะ publish directory คือ `.` (root) ใครก็เปิด
  `https://<site>/netlify/functions/fred-data.js`, `/README.md`, `/tests/...`, `/package.json` ดูได้
  (ไม่มี API key รั่ว เพราะ key อยู่ใน env var แต่ไม่ควรให้อ่านซอร์ส/บันทึกภายใน) → เพิ่มไฟล์ `_redirects`
  บล็อก `/netlify/*`, `/tools/*`, `/tests/*`, `/*.md`, `/package.json`, `/netlify.toml` ด้วย 404
  (พาธ Functions จริงคือ `/.netlify/functions/*` มีจุดนำหน้า จึงไม่ถูกบล็อก)
- **`netlify.toml`**: pin `NODE_VERSION=20` (โค้ดใช้ `fetch`/`AbortController` ของ Node 18+ ทั้งหมด)
  + `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` + `index.html` แบบ `must-revalidate`
  **จงใจไม่ใส่ `X-Frame-Options`/`frame-ancestors`** เพราะหน้าเว็บนี้ถูกเปิดใน preview iframe (Arena/Netlify Deploy Preview) จะพังทันที
- **เทสต์**: เพิ่มชุดเทสต์ `node:test` 32 เคส (`npm test`, ไม่ต้องติดตั้งอะไรเพิ่ม) ครอบคลุม parser
  FRED CSV / FRED API / BLS / Google News RSS, การเทียบ YoY ตามวันที่จริง (เคสเดือน ต.ค. 2025 หาย),
  payrolls, ปฏิทิน FOMC, การแยกค่า BGeometrics และ **smoke test ของ index.html** (รันสคริปต์ inline
  ทุกก้อนกับ DOM จำลอง แล้วฟ้องถ้าพัง + ตรวจว่า id ที่อ้างถึงมีจริง + กันบั๊กเก่าๆ กลับมา เช่น @import
  ผิดตำแหน่ง, #today มีคนเขียนหลายที่, FOMC 2027 หาย, ลืม esc() ข้อความจาก feed)

## 3) 🟠 ยังไม่ได้แก้ — ต้องให้คุณตัดสินใจ

### 3.1 ข้อความวิเคราะห์เศรษฐกิจแบบ hardcode (น่าจะเป็นเรื่องที่คุณกังวลที่สุด)
ในหน้าเว็บมีการ์ดวิเคราะห์ 4 หัวข้อที่ **เขียนค้างไว้ตั้งแต่รอบประกาศ ก.ค. 2026** (NFP −23,000, CPI +3.4%,
FOMC คงดอกเบี้ย 3.50–3.75%) วางอยู่ใต้ตัวเลขที่อัปเดตสด — มีคำเตือนกำกับแล้วว่า "อาจไม่ตรงกับการ์ด"
แต่มันจะยิ่งไม่ตรงขึ้นทุกเดือน มี 3 ทางเลือก (ผมยังไม่แตะ เพราะเป็นการตัดสินใจด้านเนื้อหา ไม่ใช่เทคนิค):

1. **ซ่อนอัตโนมัติ** เมื่อตัวเลขสดต่างจากที่เขียนไว้เกิน threshold — ปลอดภัยที่สุด งานน้อย
2. **ฉีดตัวเลขสดเข้าไปในข้อความ** (เช่นแทน `+3.4%` ด้วยค่าจาก `cpi.value`) — ข้อความจะจริงเสมอ
   แต่บทวิเคราะห์ (9-3 เสียง, consensus) ยังเป็นของเดือนนั้นอยู่ดี → ต้องเขียนใหม่ทุกรอบประกาศ
3. **ย้ายเป็น "บทวิเคราะห์รอบล่าสุด"** พร้อมกำกับเดือนชัดเจนในหัวข้อ + ปุ่มพับเก็บ

> แนะนำ: ทางเลือก 1 หรือ 3 — เพราะตัวเลขการ์ดด้านบนคือของจริงอยู่แล้ว ส่วนข้อความควรบอกชัดว่าเป็น
> "บันทึกของรอบประกาศหนึ่งๆ" ไม่ใช่คำอธิบายของตัวเลขปัจจุบัน

### 3.2 Content-Security-Policy
ยังไม่ได้ใส่ CSP เพราะหน้านี้ดึงจากโดเมนภายนอกหลายเจ้า (TradingView, Binance ×2, OKX, CoinGecko,
Hyperliquid, allorigins, MyMemory, Google News, Google Fonts, FRED, BLS, CMC, alternative.me, mempool.space,
frankfurter) — ถ้าเขียน whitelist พลาดแม้เจ้าเดียว ฟีเจอร์นั้นจะตายเงียบๆ แนะนำให้ทำเป็น
`Content-Security-Policy-Report-Only` แล้วดู report สัก 1–2 สัปดาห์ก่อนบังคับใช้จริง

## 4) 🔵 ปรับได้ก็ดี (ยังไม่ทำ)

| เรื่อง | ทำไมควรทำ | ความเสี่ยงถ้าทำ |
|---|---|---|
| ย้าย Open Interest (CoinGecko `/derivatives`) ไปแคชฝั่งเซิร์ฟเวอร์ | payload ใหญ่ และทุกคนยิงเอง (CoinGecko ฟรีมี rate limit ต่อ IP) | เพิ่ม dependency ต่อ function ใหม่ |
| ลดจำนวน request ของ `fred-data` (CPI + Core CPI = 4 คำขอ/รอบ เพราะขอ `pc1` + `levels` คนละชุด) | FRED graph CSV รองรับหลาย series ในคำขอเดียว → เหลือ 2 คำขอ | ตัว fallback chain ต่อ series จะซับซ้อนขึ้นเล็กน้อย |
| เพิ่ม rate limit / จำกัด origin ของ functions (`Access-Control-Allow-Origin: *` ตอนนี้) | กันคนอื่นเอา `news-feed`/`oil-price` ไปใช้เป็น proxy ฟรี | ต้องมี allowlist โดเมนจริง |
| เพิ่ม CI รัน `npm test` อัตโนมัติ (GitHub Actions) | กันเทสต์เน่าถ้าแก้โค้ดต่อในอนาคต | ไม่มี |
| ลบ/ย้ายไฟล์ภายในออกจาก publish dir แทนที่จะใช้ `_redirects` | publish dir สะอาดกว่า | ต้องย้าย index.html เข้าโฟลเดอร์ย่อย = เปลี่ยน URL ที่ deploy อยู่ (ผลกระทบมากกว่า) |
| ตั้ง `BGEOMETRICS_TOKEN` ใน Netlify | โควต้า 8/ชม. · 15/วัน ต่อ IP → ใช้ร่วมกันทั้งเว็บ | ไม่มี (ไม่ใส่ก็ทำงานได้) |
| อีเมล `global.intelligence.cache@gmail.com` โผล่ในซอร์สหน้าเว็บ (ส่งให้ MyMemory เพื่อเพิ่มโควต้าแปล) | ถ้าไม่อยากให้อีเมลตัวเองสาธารณะ ให้แก้ที่ค่าคงที่ `TRANSLATE_EMAIL` ใน `index.html` ที่เดียว | โควต้าแปลต่อวันจะลดลง (แอปยังทำงานปกติ) |

## 5) เรื่องที่ตรวจแล้ว "ไม่ต้องแก้"

- **ค่า Fed Rate / CPI / Unemployment ที่ hardcode ไว้ใน HTML** — เป็นแค่ placeholder ก่อนข้อมูลจริงมาถึง
  ตัวสคริปต์เขียนทับทันทีที่ดึงสำเร็จ (ยืนยันจาก `applyFedData()`)
- **ตาราง halving (17 เม.ย. 2028, block 1,050,000)** — ยังห่างไกล และมี block height จริงมาปรับให้ทุกวันอยู่แล้ว
- **`Alpha Vantage WTI`** — ตรวจเอกสารไม่พบประกาศยกเลิก endpoint (บางเจ้าอ้างว่ายังใช้ได้ในแผนฟรี)
  จึงคงไว้เป็น fallback ชั้นสุดท้าย แต่แก้ให้รายงานข้อความต้นทางตรงๆ แล้ว ถ้าตายจะรู้ทันที
- **Binance `CLUSDT` / OKX `CL-USDT-SWAP` / Hyperliquid `XYZ:CL-USD`** — เป็นสัญญาณ WTI จริงทั้งหมด
  (Binance เปิดตัว เม.ย. 2026, OKX มี.ค. 2026, Hyperliquid ผ่าน builder trade.xyz) ลำดับการไล่แหล่งถูกต้องแล้ว

## 6) วิธีตรวจหลัง deploy (5 นาที)

```bash
npm test                      # 26/26 ต้องผ่าน (ไม่ต้องต่อเน็ต)
node --check netlify/functions/*.js
```

แล้วเปิด URL เหล่านี้บนเว็บจริง:

| เปิดอะไร | ควรเห็น |
|---|---|
| `/.netlify/functions/fred-data` | JSON มีตัวเลขจริง + `source` = `fred-csv`/`fred-api`/`bls` และ `nfp` มีค่า (ถ้าเป็น `errors.nfp` = ยังต้องดู log) |
| `/.netlify/functions/news-feed?q=opec` | `{"status":"ok","source":"google-news","items":[…]}` |
| `/.netlify/functions/onchain?metric=nupl` | `{"metric":"nupl","value":…,"source":"bgeometrics"}` (ถ้า 502 = โควต้าหมด/ต้องตั้ง `BGEOMETRICS_TOKEN`) |
| `/.netlify/functions/cmc-fear-greed` | `{"value":…,"source":"coinmarketcap"}` |
| หน้าแรก กด `F12 → Network` แล้วโหลดหน้า | ควรเห็น **ไม่มี** คำขอ `s3.tradingview.com` จนกว่าจะกดแท็บ BTC และเห็นฟอนต์ `Space Grotesk`/`JetBrains Mono` ถูกโหลดจาก `fonts.gstatic.com` |
| กดแท็บ BTC แล้วดูช่องราคา | พิมพ์ราคาเอง → กด "รีเฟรชทั้งหมด" → ค่าที่พิมพ์ต้องไม่ถูกทับ |
| เปิด `/#btc` | ต้องเข้าแท็บ BTC ตรงๆ และมีกราฟขึ้น |

## 7) ไฟล์ที่แก้ในรอบนี้

```
index.html                     (แก้เยอะสุด — ฟอนต์, XSS, แคชข่าว, deep link, lazy chart, FOMC 2027, ราคา/ATH)
netlify.toml                   (Node 20 + security headers)
netlify/functions/fred-data.js (BLS payrolls id, FOMC 2027, แยกฟังก์ชันบริสุทธิ์เพื่อเทสต์)
netlify/functions/news-feed.js (เขียนใหม่: อ่าน RSS เอง + fallback)
netlify/functions/oil-price.js (timeout + รายงานสาเหตุจริง + แคช CDN)
netlify/functions/cmc-fear-greed.js (กันค่าเพี้ยน + ไม่เอา time_until_update มาเป็น timestamp)
netlify/functions/onchain.js   (ใหม่)
+ fred-data.test.js, news-feed.test.js, onchain.test.js, tests/index-html.test.js (ใหม่)
+ _redirects, tools/dev-server.js, package.json (ใหม่ — dev/test เท่านั้น ไม่เกี่ยวกับ deploy)
README.md                      (โครงสร้างไฟล์, env vars, รันเทสต์, สรุปรอบแก้)
```

> ทุกการแก้ถูก commit ไว้ใน branch นี้แล้ว — ดูสรุปได้ด้วย `git show --stat HEAD` หรือเทียบกับ main
> ด้วย `git diff main --stat` · ยังไม่ merge ขึ้น main และถ้าต้องการให้เปิด Pull Request บอกได้ครับ
